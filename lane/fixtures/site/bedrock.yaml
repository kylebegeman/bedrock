app: site
description: The lane's static fixture
owner: personal
workloads:
  pages:
    kind: static
    dir: public
    routes:
      - host: site.lane.example.com
checks:
  - url: https://site.lane.example.com/
    contains: A static site served by bedrock
